<?xml version="1.0" encoding="utf-8"?>
<zone>
  <short>My zone</short>
  <description>Unsolicited incoming network packets are rejected from port 1 to 1024, except for select network services. Incoming packets that are related to outgoing network connections are accepted. Outgoing network connections are allowed.</description>
  <port port="1025-65535" protocol="udp"/>
  <port port="1025-65535" protocol="tcp"/>
  <!-- allow ssh connections to a local vm only -->
  <rule family="ipv4">
    <source address="192.168.10.1"/>
    <port port="23" protocol="tcp"/>
    <reject/>
  </rule>
  <!-- Block icmp messages that could be used by an attacker to do footprinting -->
  <icmp-block name="echo-request"/>
  <icmp-block name="echo-reply"/>
  <icmp-block name="timestamp-request"/>
  <icmp-block name="timestamp-reply"/>
  <forward/>
</zone>
